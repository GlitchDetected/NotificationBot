# Configure

```bash
git clone https://github.com/glitchdetected/notificationbot.git
# or git pull if you already did
sudo nano .env
# paste in the stuff from .env.example
docker-compose -f docker-compose.yml up

# for mac users
find . -name ".DS_Store" -delete

```